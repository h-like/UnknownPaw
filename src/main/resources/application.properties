# settings for web application
# App name
spring.application.name=unknownPaw

# Server port
server.port=8080

# Context path
server.servlet.context-path=/unknownPaw

# Restart WAS
spring.devtools.livereload.enabled=true

# Spring Datasource
#spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
#spring.datasource.url=jdbc:mariadb://localhost:3306/db8
#spring.datasource.username=db8
#spring.datasource.password=1234

# Render
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JPA
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# File upload setting for Servlet
spring.servlet.multipart.enabled=true
spring.servlet.multipart.location=C:\\upload
spring.servlet.multipart.max-request-size=30MB
spring.servlet.multipart.max-file-size=10MB

# File upload Constant for Application
upload.base-path=${UPLOAD_PATH:default}

kakao.maps.api-url=https://dapi.kakao.com
kakao.maps.rest-api-key=736b05ff65d63994925956f8c5b6d043

# WebClient ??
spring.webflux.client.timeout=30s
spring.webflux.client.max-in-memory-size=10MB

# ?? ??
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=1000,expireAfterWrite=24h

# CORS ??
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# API ?? ?? ??
kakao.maps.rate-limit=100
kakao.maps.rate-limit-period=1

# ???? ??
management.endpoints.web.exposure.include=health,metrics,prometheus
management.metrics.export.prometheus.enabled=true
management.endpoint.health.show-details=always

# ?? ??
logging.level.com.unknownpaw=DEBUG
logging.level.com.seroter.unknownPaw.service.KakaoMapsService=TRACE
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.file.name=logs/unknownpaw.log
logging.file.max-size=10MB
logging.file.max-history=30
logging.level.org.springframework.web.servlet.resource=DEBUG

# ?? ??
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true

# ?? ??
spring.security.filter.order=10
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}

# ?? ??
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never

# HTTP ????? ??
spring.http.client.max-connections=100
spring.http.client.max-connections-per-route=20
spring.http.client.connection-timeout=5000
spring.http.client.read-timeout=5000

# ??? ??
spring.profiles.include=oauth

# Swagger ?? (API ???)
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

#?/????
#spring.web.resources.static-locations=file:/Users/hozi_studio/upload/,classpath:/static/
spring.web.resources.static-locations=file:/C:/upload/,classpath:/static/

# ???? spring.web.resources.static-locations=file:///C:/upload/,classpath:/static/

#com.seroter.upload.path=/Users/hozi_studio/upload
com.seroter.upload.path=C:/upload